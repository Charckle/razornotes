{% extends "peopleflow/base.html" %}

{% block extraheader %}
{% endblock %}

{% block content%}

  <main>
  <br>
  
  <nav aria-label="breadcrumb">
	  <ol class="breadcrumb">
		<li class="breadcrumb-item"><a href="{{ url_for('peopleflow.index') }}">Home</a></li>
	    <li class="breadcrumb-item active" aria-current="page">Skupina</li>
	  </ol>
	</nav>
	
  <div class="row g-5">

    <div class="col-md-12">
      <div class="py-5 text-center">
        <img class="d-block mx-auto mb-4" src="{{ url_for('static', filename='brand/bootstrap-logo.svg') }}" alt="" width="72" height="57">
        <h2>{{ skupina['ime_skupine'] }} - <small>{{ skupina['opis_skupine'] }}</small></h2> 
      </div>
    </div>
    

    
    <div class="list-group">
    {%- for i in ljudje %}
	  <a href="{{ url_for('peopleflow.clovek', clovek_id=i['id']) }}" class="list-group-item list-group-item-action list-group-item-info">{{ i['ime'] }} {{ i['priimek'] }} - {{ i['vprasanje'] }}</a>
	 {% endfor %}
	 </div>
	 <hr>
	 
	 <div class="list-group">
	  <a href="{{ url_for('peopleflow.new_clovek') }}" class="list-group-item list-group-item-action list-group-item-success">Dodaj Človeka</a>
	  <a href="{{ url_for('peopleflow.edit_skupina', skupina_id=skupina['id']) }}" class="list-group-item list-group-item-action list-group-item-warning">Uredi Skupino</a>
	 </div>
	
  </div>
  </main>
{% endblock %}

